
# the prefix 0b can be used to represent binary values

x = 0b1010

# the prefix 0x is for hex values
y = 0xA # 10

# the prefix 0o is for octal (base 8)
z = 0o12 # 10

print(x, y, z)

print(bin(x), hex(y), oct(z))